/**
 * The Stream Package porpouse is to handle all the IO operation with {@link stream.StreamCore} and divide them for reconstruct a file with stream.input package
 * or split a file with stream.output package and the specialized class for the type of operations (split,crypt,compress)
 *
 * all the operation are called by  {@link job} package
 * @author Meschio
 *
 */
package stream;