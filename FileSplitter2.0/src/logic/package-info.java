/**
 * The package Logic handle all the hard logic of the program, like the definition of the list elements via {@link logic.FileElement}
 * the Observable list manipulator {@link logic.FSlistFileElement}, the header package for the header creatioin and reading,
 * and the {@link logic.FSPasswordHash} to handle the crypt/decrypt operations
 *
 * @author Meschio
 *
 */
package logic;