/**
 * The package Logic handle all the hard logic of the program, like the definition of the list elements via {@link @FileElement}
 * the Observable list manipulator {@link @FSlistFileElement}, the header package for the header creatioin and reading,
 * and the {@link @FSPasswordHash} to handle the crypt/decrypt operations
 *
 */
/**
 * @author Meschio
 *
 */
package logic;